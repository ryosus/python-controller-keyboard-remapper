# python-controller-keyboard-remapper

main.py > used with ViGEm driver to duplicate controller inputs

mirrored.py > basically main.py but the left and right joystick inputs are mirrored (joystickInput * -1)
